# SESI_LP
Disciplina de Lógica de Programação SESI 2026

# SESI_LP
Disciplina de Lógica de Programação SESI 2026

---

## 💻 Comandos básicos - Git Bash

- `mkdir nome_da_pasta` → criar pasta  
- `clear` → limpar tela  
- `git config --list` → ver configurações  
- `git config --global user.name "seu nome"` → adicionar nome  
- `git config --global user.email "seu email"` → adicionar email  

---

## 🌐 Comandos básicos - GitHub

- `cd nome_da_pasta` → entrar na pasta  
- `git clone link_do_repositório` → baixar projeto  
- `git status` → ver alterações  
- `git add .` → adicionar arquivos  
- `git commit -m "descrição"` → salvar alterações  
- `git push` → enviar para o GitHub  
- `git pull` → atualizar projeto  

> *Para alterações manuais*

# comandos básicos GITBASH:
mkdir nome_da_pasta (criar pasta)
clear (limpar tela)
git config --list (ver o que já tem)
git config --global user.name "seu nome" (adicionar nome)
git config --global user.email "seu email" (adicionar email)

# comandos básicos GITHUB:
cd nome_da_pasta (para entrar na pasta)
git clone link_do_repositório (para baixar um projeto)
git status (para ver o que mudou)
git add (para adicionar arquivos)
git commit -m "descrição" (para salvar alterações)
git push (para enviar para o GitHub)
git pull (para atualizar projeto) *para alteração manual
